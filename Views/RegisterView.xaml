<UserControl x:Class="BeerZdec.Views.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeerZdec.Views"
        xmlns:helpers="clr-namespace:BeerZdec.Helpers"
        mc:Ignorable="d">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Background="White">
        <StackPanel Width="300" Margin="20">
            <TextBlock Text="Регистрация" FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBlock Text="Логин:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Пароль:" Margin="0,0,0,5"/>
            <PasswordBox helpers:PasswordBoxHelper.BoundPassword="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Подтвердите пароль:" Margin="0,0,0,5"/>
            <PasswordBox helpers:PasswordBoxHelper.BoundPassword="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,0,20" Padding="5"/>

            <Button Content="Зарегистрироваться" Command="{Binding RegisterCommand}" 
                    Margin="0,0,0,10" Padding="10,5"/>

            <TextBlock HorizontalAlignment="Center">
                <Run Text="Уже есть аккаунт? "/>
                <Hyperlink Command="{Binding NavigateToLoginCommand}">
                    Войти
                </Hyperlink>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
